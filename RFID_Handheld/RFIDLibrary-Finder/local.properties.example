# ============================================================
# 本地配置模板（wechat.* 部分）
#
# 重要：本仓库的 local.properties 由 Android Studio 首次打开工程时自动生成，
#       其中包含一行 sdk.dir=...（指向你机器上的 Android SDK），【请勿整体替换该文件】，
#       否则会丢失 sdk.dir 导致 "SDK location not found"。
#       正确做法：在现有 local.properties 末尾【追加】下面的 wechat.* 三行。
#
# 用途：微信云开发直连所需的凭证。Android PDA 通过 appid+appsecret 换取 access_token，
#       再调用云函数 HTTP 接口 invokecloudfunction。
# 安全提示：appsecret 会打包进客户端 APK，请勿用于多租户/商业发布场景。
# 注意：local.properties 已被 .gitignore 忽略，不会入库。
# ============================================================

# ↓↓↓ 以下内容追加到现有 local.properties 末尾（保留其 sdk.dir 行） ↓↓↓

# 微信小程序 AppID
wechat.appId=wxYOUR_APPID

# 微信小程序 AppSecret（注意保密）
wechat.appSecret=YOUR_APPSECRET

# 微信云开发环境 ID（env，形如：family-library-1a2b3c）
wechat.envId=YOUR_ENV_ID
